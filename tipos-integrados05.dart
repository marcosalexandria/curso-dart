void main(){
  // sets
  // em um conjunto set, não pode haver valores duplicados, o set não tem ordem, então não podemos acessar os valores por índice

  Set<String> nomes = {'Lucas', 'João', 'Maria', 'Ana', 'Lucas'}; // o valor 'Lucas' só aparece uma vez no conjunto
  
  Set<String> nomes2 = Set(); // criando um set vazio
  Set<String> nomes3 = {}; // criando um set vazio
  
  print(nomes); 
}